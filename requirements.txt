# RIGEL Business — Python dependencies
# Install: pip install -r requirements.txt
#
# PyQt6 is the ONLY runtime dependency.
# Everything else (sqlite3, hashlib, uuid, datetime, json, base64,
# platform, pathlib) is Python standard library — no install needed.

PyQt6>=6.6.0
pyinstaller>=6.3.0   # packaging only — not bundled into the app
