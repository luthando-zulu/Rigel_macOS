<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  RIGEL Business — macOS Entitlements
  Required for Hardened Runtime + Notarisation.
  Stella Lumen (Pty) Ltd
-->
<plist version="1.0">
<dict>
    <!-- Allow JIT compilation (required by some Qt internals) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (PyInstaller bootloader needs this) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow dyld environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable library validation (needed for PyQt6 .so bundles) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network client (for future cloud features) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access: user-selected files (for open/save dialogs) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
